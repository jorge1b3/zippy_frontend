const domain = "http://localhost"
const url_api = `${domain}:8080/api`
export const url_vehicle = `${url_api}/vehicle`
export const url_auth = `${url_api}/auth`
export const url_stations = `${url_api}/stations`